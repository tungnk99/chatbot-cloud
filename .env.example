# ========== Chatbot (Backend/chatbot) ==========
# OPENAI_API_KEY=bỏ trống hoặc dùng Secret Manager
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# URL dịch vụ Tools (Backend/tools) - khi chạy local: http://localhost:8081
TOOLS_BASE_URL=http://localhost:8081

# Storage: local | gcs
STORAGE_BACKEND=local
LOCAL_STORAGE_PATH=./data/sessions
# GCS_BUCKET=chatbot-cloud-xxx-sessions

# Pub/Sub: xử lý chat bất đồng bộ (tránh treo khi LLM nặng). Trên GCP Terraform tự set.
# USE_PUBSUB_ASYNC=false
# PUBSUB_TOPIC=projects/PROJECT_ID/topics/chat-requests

# ========== Frontend (Streamlit) ==========
# URL Chatbot API - khi chạy local: http://localhost:8080
CHATBOT_API_URL=http://localhost:8080
# Mock API: true/1 để test giao diện không cần backend (trả về dữ liệu giả)
# MOCK_API=false

# ========== Tools (Backend/tools) ==========
# Không cần biến môi trường đặc biệt
